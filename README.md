# download-video-python

- pip install urllib3 

To work a application, insert  the url's inside of array 'url links' separate for ''

- python main.py

- * download criado para sites que possuem a url no nome *